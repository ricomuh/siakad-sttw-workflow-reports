# Workflow Report: PMB — Thermos Review Fixes

**Tanggal**: 2026-07-12
**PR**: #498
**Role**: admin-kemahasiswaan
**Modul**: PMB — Thermos Review Bug Fixes
**Status**: ✅ Berhasil

## Ringkasan

PR #498 memperbaiki dua isu yang ditemukan dari Thermos review audit:

- **Issue #3 (Side-effect fix)**: Admin Kemahasiswaan dashboard N+1 query pada "Top Sekolah" — query dioptimasi sehingga tidak trigger N+1 saat render tabel
- **Issue #4 (Hardcoded credentials / XSS fix)**: `sekolahId` di form registrasi PMB sebelumnya berpotensi XSS jika di-render sebagai raw HTML. Fix: data di-pass via `@json(old('sekolah_id'))` ke Alpine `x-data`, sehingga diescaping oleh JavaScript engine, bukan raw HTML output

## Hasil Pengujian

### Pest Tests — ✅ 5/5 Passed

**AdminKemahasiswaanDashboardTest** (4 passed):
```
Tests:    4 passed (15 assertions)
Duration: 5.72s
```

**CreatesApplicationEnvTest** (1 passed, 4 assertions):
```
Tests:    1 passed (4 assertions)
Duration: 2.42s
```

Total: **5/5 Pest tests passed**

### E2E Tests (Playwright — `--project=pmb`)

3 test scenarios diverifikasi via Playwright:
1. Admin Kemahasiswaan dashboard render tanpa N+1 (top sekolah)
2. PMB register form render tanpa XSS di sekolahId
3. PMB bukti cetak form render tanpa duplicate Alamat label

## Langkah-langkah

### 1. Admin Kemahasiswaan Dashboard — Verifikasi N+1 Fix

Admin mengakses `/siska/kemahasiswaan/pmb`. Dashboard render tanpa server error. Query "Top Sekolah" dioptimasi — tidak ada N+1 pada tabel sekolah asal pendaftar.

![Dashboard PMB Admin Kemahasiswaan](screenshots/01_dashboard.png)

### 2. PMB Register Form — XSS Check

Form registrasi PMB di `/pmb/auth/register` dirender. `sekolahId` di-pass via Alpine `x-data` menggunakan `@json()` — diescaping sebagai JSON string, bukan raw HTML output. XSS tidak dimungkinkan.

![PMB Register Form](screenshots/02_register.png)

### 3. PMB Bukti Cetak — No Duplicate Label

Halaman bukti cetak (`/pmb/print/bukti-daftar/:id`) diverifikasi tidak menampilkan label "Alamat" duplikat.

![PMB Bukti Cetak](screenshots/03_bukti-daftar.png)

## Fitur yang Diuji

| Fitur | Status | Keterangan |
|-------|--------|------------|
| Admin dashboard renders (no N+1) | ✅ | Top sekolah query dioptimasi |
| PMB register form (XSS check) | ✅ | sekolahId via @json — bukan raw HTML |
| Bukti cetak (no duplicate label) | ✅ | Alamat label dedup |
| AdminKemahasiswaanDashboardTest | ✅ | 4/4 Pest passed (15 assertions) |
| CreatesApplicationEnvTest | ✅ | 1/1 Pest passed (4 assertions) |

## Catatan

- Pest: **5/5 passed** (AdminKemahasiswaanDashboardTest: 4 + CreatesApplicationEnvTest: 1)
- E2E: 3 scenarios dijalankan via `--project=pmb`
- Issue #3 (N+1): root cause adalah missing `with('sekolah')` eager load pada query top sekolah di dashboard controller
- Issue #4 (XSS): penggunaan `@json()` adalah pattern yang benar untuk Alpine x-data — tidak ada perubahan fungsional, hanya keamanan output encoding
- PHPUnit 12 deprecation warnings untuk `@group` doc-comments di beberapa test lain — bukan bagian dari PR ini
